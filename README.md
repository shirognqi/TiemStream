# Job List
正在写列表模板
	/var/www/html/joblist/Backend/smarty/templates
从上面的目录经常到`cd ../../../Front/`下，里面有js和css，并且bootstrap的js和css是单独在bootstrap文件夹中的;


一个想法：
那个state是否有状态，就是是否参与排序




php 编译的
./configure  --prefix=/usr/local/php -enable-fpm --with-mysqli --with-config-file-path=/usr/local/etc --enable-pdo --enable-mysqlnd --with-pdo-mysql


mysql 的字符集
`CREATE DATABASE IF NOT EXISTS yourdbname DEFAULT CHARSET utf8 COLLATE utf8_general_ci;`
